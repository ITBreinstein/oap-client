# .gitignore is honoured automatically; these two are tracked but not ours to format.
pnpm-lock.yaml
docs/architecture.svg

# Payloads captured verbatim from a live pygeoapi. Reformatting them would
# destroy the evidence: the point is that they are byte-for-byte what the
# server sent, so a diff after re-capturing means the server changed.
packages/core/test/fixtures/

# Execute requests copied from the scenario repository, same reasoning: a diff
# here should mean the capture changed, not that a formatter ran. The JSON only;
# the README beside them is ours. See that README for provenance.
apps/web/test/fixtures/forms/**/*.json
